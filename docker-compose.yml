networks:
  diomede-net:
    driver: bridge

volumes:
  orthanc-us-data:
  orthanc-eu-data:
  orthanc-asia-data:
  orthanc-af-data:
  orthanc-edge-data:


services:
  # Cloud DICOM Node - US East (GCP us-east1 simulation)
  orthanc-us:
    image: orthancteam/orthanc:26.4.2
    container_name: ${REGION1_HOST}
    ports:
      - "${REGION1_HTTPS_PORT}:${ORTHANC_HTTPS_PORT}"
      - "${REGION1_DICOM_PORT}:${ORTHANC_DICOM_PORT}"
    env_file: .env
    entrypoint: /bin/sh
    command:
      - -c
      - |
        sed "s/\$${orthanc_user}/$$ORTHANC_USER/g; s/\$${orthanc_password}/$$ORTHANC_PASSWORD/g" \
          /etc/orthanc/orthanc.template.json > /tmp/orthanc.json
        exec Orthanc /tmp/orthanc.json
    volumes:
      - ./config/orthanc/us.template.json:/etc/orthanc/orthanc.template.json:ro
      - ./certs/orthanc-us:/etc/orthanc/certs:ro
      - orthanc-us-data:/var/lib/orthanc/db
    cap_add:
      - NET_ADMIN
    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, base64, os
          ctx = ssl.create_default_context(cafile='/etc/orthanc/certs/ca.pem')
          r = urllib.request.Request('https://localhost:8042/system')
          r.add_header('Authorization', 'Basic ' + base64.b64encode(
              (os.environ['ORTHANC_USER'] + ':' + os.environ['ORTHANC_PASSWORD']).encode()
          ).decode())
          with urllib.request.urlopen(r, context=ctx, timeout=5) as response: pass
      interval: 10s
      timeout: 5s
      retries: 3

  # Cloud DICOM Node - EU West (GCP eu-west1 simulation)
  orthanc-eu:
    image: orthancteam/orthanc:26.4.2
    container_name: ${REGION2_HOST}
    ports:
      - "${REGION2_HTTPS_PORT}:${ORTHANC_HTTPS_PORT}"
      - "${REGION2_DICOM_PORT}:${ORTHANC_DICOM_PORT}"
    env_file: .env
    entrypoint: /bin/sh
    command:
      - -c
      - |
        sed "s/\$${orthanc_user}/$$ORTHANC_USER/g; s/\$${orthanc_password}/$$ORTHANC_PASSWORD/g" \
          /etc/orthanc/orthanc.template.json > /tmp/orthanc.json
        exec Orthanc /tmp/orthanc.json
    volumes:
      - ./config/orthanc/eu.template.json:/etc/orthanc/orthanc.template.json:ro
      - ./certs/orthanc-eu:/etc/orthanc/certs:ro
      - orthanc-eu-data:/var/lib/orthanc/db
    cap_add:
      - NET_ADMIN
    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, base64, os
          ctx = ssl.create_default_context(cafile='/etc/orthanc/certs/ca.pem')
          r = urllib.request.Request('https://localhost:8042/system')
          r.add_header('Authorization', 'Basic ' + base64.b64encode(
              (os.environ['ORTHANC_USER'] + ':' + os.environ['ORTHANC_PASSWORD']).encode()
          ).decode())
          with urllib.request.urlopen(r, context=ctx, timeout=5) as response: pass
      interval: 10s
      timeout: 5s
      retries: 3

  # Cloud DICOM Node - Asia Northeast (GCP asia-northeast1 simulation)
  orthanc-asia:
    image: orthancteam/orthanc:26.4.2
    container_name: ${REGION3_HOST}
    ports:
      - "${REGION3_HTTPS_PORT}:${ORTHANC_HTTPS_PORT}"
      - "${REGION3_DICOM_PORT}:${ORTHANC_DICOM_PORT}"
    env_file: .env
    entrypoint: /bin/sh
    command:
      - -c
      - |
        sed "s/\$${orthanc_user}/$$ORTHANC_USER/g; s/\$${orthanc_password}/$$ORTHANC_PASSWORD/g" \
          /etc/orthanc/orthanc.template.json > /tmp/orthanc.json
        exec Orthanc /tmp/orthanc.json
    volumes:
      - ./config/orthanc/asia.template.json:/etc/orthanc/orthanc.template.json:ro
      - ./certs/orthanc-asia:/etc/orthanc/certs:ro
      - orthanc-asia-data:/var/lib/orthanc/db
    cap_add:
      - NET_ADMIN
    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, base64, os
          ctx = ssl.create_default_context(cafile='/etc/orthanc/certs/ca.pem')
          r = urllib.request.Request('https://localhost:8042/system')
          r.add_header('Authorization', 'Basic ' + base64.b64encode(
              (os.environ['ORTHANC_USER'] + ':' + os.environ['ORTHANC_PASSWORD']).encode()
          ).decode())
          with urllib.request.urlopen(r, context=ctx, timeout=5) as response: pass
      interval: 10s
      timeout: 5s
      retries: 3

  # Cloud DICOM Node - Africa South (GCP af-south1 simulation)
  orthanc-af:
    image: orthancteam/orthanc:26.4.2
    container_name: ${REGION4_HOST}
    ports:
      - "${REGION4_HTTPS_PORT}:${ORTHANC_HTTPS_PORT}"
      - "${REGION4_DICOM_PORT}:${ORTHANC_DICOM_PORT}"
    env_file: .env
    entrypoint: /bin/sh
    command:
      - -c
      - |
        sed "s/\$${orthanc_user}/$$ORTHANC_USER/g; s/\$${orthanc_password}/$$ORTHANC_PASSWORD/g" \
          /etc/orthanc/orthanc.template.json > /tmp/orthanc.json
        exec Orthanc /tmp/orthanc.json
    volumes:
      - ./config/orthanc/af.template.json:/etc/orthanc/orthanc.template.json:ro
      - ./certs/orthanc-af:/etc/orthanc/certs:ro
      - orthanc-af-data:/var/lib/orthanc/db
    cap_add:
      - NET_ADMIN
    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, base64, os
          ctx = ssl.create_default_context(cafile='/etc/orthanc/certs/ca.pem')
          r = urllib.request.Request('https://localhost:8042/system')
          r.add_header('Authorization', 'Basic ' + base64.b64encode(
              (os.environ['ORTHANC_USER'] + ':' + os.environ['ORTHANC_PASSWORD']).encode()
          ).decode())
          with urllib.request.urlopen(r, context=ctx, timeout=5) as response: pass
      interval: 10s
      timeout: 5s
      retries: 3

  # Orchestrator: FastAPI service with Telemetry Daemon
  orchestrator:
    build:
      context: .
      dockerfile: ./config/orchestrator/Dockerfile
    container_name: orchestrator
    ports:
      - "${ORCHESTRATOR_HTTPS_PORT}:${ORCHESTRATOR_PORT}"
      # - "${REDIS_PORT}:${REDIS_PORT}"
    # All runtime config (REDIS_URL, DAEMON_POLL_INTERVAL_S, scoring weights,
    # NODE_*_BASE/USER/PASS, REQUESTS_CA_BUNDLE, ...) comes from .env via
    # env_file below.
    env_file: .env

    volumes:
      - ./certs/orchestrator/server.crt:/certs/server.crt:ro
      - ./certs/orchestrator/server.key:/certs/server.key:ro
      - ./certs/ca.pem:/certs/ca.pem:ro

    depends_on:
      orthanc-us:
        condition: service_healthy
      orthanc-eu:
        condition: service_healthy
      orthanc-asia:
        condition: service_healthy
      orthanc-af:
        condition: service_healthy

    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, sys
          ctx = ssl.create_default_context(cafile='/certs/ca.pem')
          try:
            urllib.request.urlopen('https://localhost:8000/health', context=ctx, timeout=5)
            sys.exit(0)
          except Exception:
            sys.exit(1)
      interval: 10s
      timeout: 5s
      retries: 3

  # Edge Agent: Edge Orthanc + Forwarder Daemon
  edge-agent:
    build:
      context: .
      dockerfile: ./config/edge/Dockerfile
    container_name: edge-agent
    ports:
      - "${EDGE_AGENT1_HTTPS_PORT}:${ORTHANC_HTTPS_PORT}"  # Edge Orthanc REST API
      - "${EDGE_AGENT1_DICOM_PORT}:${ORTHANC_DICOM_PORT}"  # Edge Orthanc DICOM
    env_file: .env
    volumes:
      - ./config/orthanc/edge.template.json:/etc/orthanc/orthanc.template.json:ro
      - ./certs/edge-agent:/etc/orthanc/certs:ro
      - ./certs/ca.pem:/certs/ca.pem:ro
      - orthanc-edge-data:/var/lib/orthanc/db
    environment:
      # EDGE_USER/PASS, NODE_*_BASE/USER/PASS, ORCHESTRATOR_API_KEY,
      # FORWARDER_POLL_INTERVAL_S, PROBE_INTERVAL_S, and REQUESTS_CA_BUNDLE
      # come from .env via env_file above. Only names that differ from
      # their .env property key, or that are composed from one, need an
      # explicit mapping here:

      # This container's own co-located Orthanc.
      EDGE_BASE: "${NODE_INT_BASE}"

      # Property key is EDGE_AGENT1; the forwarder code expects AGENT_ID.
      AGENT_ID: "${EDGE_AGENT1}"

      # Orchestrator connection.
      ORCH_URL: "${ORCHESTRATOR_BASE}/get-best-node"
      ORCH_HEARTBEAT_URL: "${ORCHESTRATOR_BASE}/heartbeat"

    depends_on:
      orchestrator:
        condition: service_healthy

    networks:
      - diomede-net
    healthcheck:
      test:
        - "CMD"
        - "python3"
        - "-c"
        - |
          import urllib.request, ssl, base64, os
          ctx = ssl.create_default_context(cafile='/etc/orthanc/certs/ca.pem')
          r = urllib.request.Request('https://localhost:8042/system')
          r.add_header('Authorization', 'Basic ' + base64.b64encode(
              (os.environ['ORTHANC_USER'] + ':' + os.environ['ORTHANC_PASSWORD']).encode()
          ).decode())
          with urllib.request.urlopen(r, context=ctx, timeout=5) as response: pass
      interval: 10s
      timeout: 5s
      retries: 3
